apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: mcp-server-template-destination-rule
spec:
  host: mcp-server-template-service
  subsets:
    - name: v1
      labels:
        version: v1
